# 워크로그 — 2026.04.20

> **목적:** 다음 세션에서 이 문서를 읽고 프로젝트 전체 context를 복원하기 위함.

---

## 오늘의 핵심 요약

**뉴스레터 → 웹매거진 피벗이 외부 환경에서 진행됐다는 사실을 뒤늦게 발견하고, 유실될 뻔한 콘텐츠를 백업하여 Vercel에 재배포. 동시에 구버전 덮어쓰기 위험이 있던 스케줄 에이전트 정지.**

---

## 상황 복원 (세션 시작 시점)

- **이전 세션 종료 상태**: 뉴스레터(단일 HTML, 다크/라이트) 버전이 `kletter.vercel.app`에 배포된 상태. 매주 금요일(로 메모에 기록) 자동 발행 스케줄 돌아가는 중.
- **다음 세션 TODO**: 웹 페이지 수정 작업 (라이트 버전 역할 분리, 다크/라이트 JS 통합, SVG 차트 등)

## 세션 중 발견한 사실 (큰 발견들)

1. **뉴스레터 → 웹매거진 피벗 진행 중이었음** — 사용자가 watcha.io 내부 ADP(App Deployment Platform)에서 새 버전을 작업 중. 이 맥에는 소스 없음.
2. **trycloudflare 임시 URL로 공개 상태였음** — `https://includes-advised-posted-lobby.trycloudflare.com`에서 새 버전 확인 가능. 접속 당시 URL 살아있었음.
3. **HTML meta 태그에 ADP 흔적** — `adp-app-name: kletter`, `adp-owner: kay`. watcha.io 내부 ADP에서 호스팅됨을 확인.
4. **스케줄 에이전트 대상 레포가 예상과 달랐음** — `kaykimw/kletter-newsletter`가 아니라 **`frograms/ai-develop-platform-apps`** (WATCHA 내부 ADP 레포). 즉 스케줄은 watcha.io 쪽을 건드림.
5. **스케줄 실제 실행일이 금요일이 아닌 수요일** — cron `0 0 * * 3` = 수요일 UTC 00:00 = KST 09:00. 기존 메모는 오류였음.

---

## 오늘 수행한 작업 (시간순)

### Phase 1: 상황 파악 및 콘텐츠 백업
1. trycloudflare URL에서 서빙 중인 페이지 확인 (`/shortform/2026-W17`)
2. URL 아직 살아있음 확인 (HTTP 200)
3. 모든 페이지 목록 파악:
   - `/`, `/weekly`, `/weekly/2026-W16`, `/weekly/2026-W17`
   - `/shortform`, `/shortform/2026-W17`
   - `/subscribe`, `/archive/2026-W16.html`
4. 8개 HTML 파일 전부 `~/kletter-shortform-backup/`에 다운로드
5. 로컬 소스/기록 수색:
   - `~/Projects`, 쉘 히스토리, Claude 세션 기록 전체 — 소스 없음 확인
   - 외부 이미지 43개는 publisher 사이트 링크라서 별도 다운로드 불필요

### Phase 2: 배포 방향 결정
6. 사용자 선택: **옵션 A — 기존 kletter.vercel.app 도메인에 웹매거진으로 교체 배포**
7. URL 구조 설계: `/weekly/2026-W17/index.html` 같은 폴더 구조로 배치해서 확장자 없이 서빙 가능하게

### Phase 3: 로컬 재구성
8. `webmagazine-pivot` 브랜치 생성 (main 보존)
9. `deploy/` 에서 구버전 HTML 삭제: `index.html`, `light.html`, `subscribe.html`
10. `deploy/api/` 는 그대로 유지 (구독 API 재사용)
11. 백업 HTML 8개를 새 폴더 구조로 배치:
    ```
    deploy/
    ├── index.html                           (홈)
    ├── weekly/index.html                    (주간 목록)
    ├── weekly/2026-W16/index.html
    ├── weekly/2026-W17/index.html
    ├── shortform/index.html
    ├── shortform/2026-W17/index.html
    ├── subscribe/index.html
    ├── archive/2026-W16.html
    └── api/subscribe.js                     (기존 유지)
    ```

### Phase 4: 로컬 프리뷰 및 검증
12. 포트 4567에서 `python3 -m http.server` 실행 (4321은 JPIRNEWS Astro dev 서버 사용 중)
13. 모든 페이지 HTTP 200 확인
14. 사용자가 브라우저로 확인 → OK

### Phase 5: 배포
15. 커밋: "웹매거진 피벗 — 뉴스레터 단일 HTML → 다중 페이지 구조 전환"
16. `main` 체크아웃 후 `webmagazine-pivot` 병합 (--no-ff, 머지 커밋 생성)
17. `git push origin main` → Vercel 자동 배포 트리거
18. 배포 후 모든 URL HTTP 200 확인 (https://kletter.vercel.app/weekly/2026-W17/ 등)
19. 로컬 프리뷰 서버 종료

### Phase 6: 스케줄 정지
20. RemoteTrigger API로 스케줄(`trig_01SAQj85Q5UgKyp4BiUaHb58`) 상태 조회
21. 발견: 스케줄이 향하는 레포는 `frograms/ai-develop-platform-apps` (ADP 내부), 실행일은 수요일
22. `enabled: false` 로 업데이트 → 4/22(수) 자동 실행 방지

### Phase 7: 메모리 갱신
23. `project_kletter.md` 전면 개정:
    - 뉴스레터 → 웹매거진 피벗 반영
    - 사이트 구조 (다중 페이지) 기록
    - watcha.io ADP 원본/Vercel 배포 관계 명시
    - 스케줄 정지 상태 반영 + 요일 오류 정정

---

## 현재 아키텍처 (배포 후)

```
┌─────────────────────────────────────────────────┐
│  소스 원본: watcha.io ADP (kletter.watcha.io)    │
│   ├ frograms/ai-develop-platform-apps 레포       │
│   └ 현재 접속 불가 상태 (배포 전 상태)             │
└─────────────────────────────────────────────────┘
              ↓ (trycloudflare로 잠깐 공개)
┌─────────────────────────────────────────────────┐
│  HTML 백업: ~/kletter-shortform-backup/          │
│  (8개 페이지 정적 HTML)                           │
└─────────────────────────────────────────────────┘
              ↓ (이번 세션에 반영)
┌─────────────────────────────────────────────────┐
│  Vercel 배포: kletter.vercel.app                 │
│   ├ 레포: kaykimw/kletter-newsletter             │
│   ├ 구버전 뉴스레터는 git 히스토리에 보존          │
│   └ 현재 main = 웹매거진 버전                     │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│  스케줄 에이전트 (정지됨)                          │
│   ├ trig_01SAQj85Q5UgKyp4BiUaHb58                │
│   ├ 대상: frograms/ai-develop-platform-apps     │
│   ├ 원래 매주 수요일 09:00 KST                    │
│   └ enabled=false (2026-04-20 기준)              │
└─────────────────────────────────────────────────┘
```

---

## watcha.io 접속 가능해지면 해야 할 것

**우선순위 높음:**

1. **ADP에서 kletter 앱 확인**
   - watcha.io ADP 대시보드에서 소유자 `kay`인 `kletter` 앱 찾기
   - 현재 배포 상태/브랜치/히스토리 확인

2. **진짜 소스 코드 접근 경로 확보**
   - ADP 레포(`frograms/ai-develop-platform-apps`)에서 `kletter/` 디렉토리 위치 파악
   - 로컬로 clone 가능한지, 권한 필요한지 확인
   - 원본 소스가 HTML인지 템플릿 엔진인지 확인 (백업 HTML은 inline style이라 소스가 별도일 가능성 높음)

3. **소스/Vercel 배포 관계 재정리**
   - watcha.io ADP와 kletter.vercel.app 중 어느 쪽을 정본으로 할지 결정
   - 두 곳 모두 유지할 경우 싱크 전략 필요
   - 옵션:
     - (a) **ADP를 정본으로** → Vercel 배포 중단, kletter.watcha.io를 공식 URL로
     - (b) **Vercel을 정본으로** → ADP는 내부 테스트 환경으로 유지
     - (c) **둘 다 유지** → 한쪽에서 수정 → 다른 쪽에 동기화 스크립트

4. **도메인 결정**
   - kletter.vercel.app vs kletter.watcha.io 둘 중 어느 것을 대표 URL로 쓸지
   - 커스텀 도메인(예: kletter.io 등) 구매 여부

**우선순위 중간:**

5. **스케줄 에이전트 재설계**
   - 현재 정지된 상태. 웹매거진 구조(다중 페이지, 주차별 디렉토리)에 맞게 프롬프트 전면 재작성 필요
   - 현재 프롬프트는 `kletter/static/index.html` (다크) + `light.html` 단일 페이지 구조 가정
   - 새 구조: 매주 `weekly/2026-WXX/` 디렉토리 생성하고 index.html 추가, 아카이브 업데이트
   - 옵션: 스케줄 아예 폐기하고 수동 발행으로 전환할지 검토

6. **`~/kletter-newsletter` 루트 정리**
   - 루트의 `newsletter_2026_W16.html`, `_light.html`, `_email.html`, `.md` 파일들 — 구버전 아티팩트. 삭제 또는 `archive/` 이동 필요.
   - `docs/research/newsletter_platform_comparison.md` — 유지 가치 있음

**우선순위 낮음:**

7. **subscribe 페이지 검토**
   - `deploy/subscribe/index.html`이 `/api/subscribe` 호출. 기존 API(`deploy/api/subscribe.js`) 재사용 가능.
   - 웹매거진 컨텍스트에 맞게 CTA 문구 수정 필요할 수 있음
   - Resend Audience는 그대로 유지 (72321aea-90c4-4875-ae44-61e9fbb50c8d)

8. **이메일 버전 정리**
   - 웹매거진 피벗 후 이메일 발송은 일단 중단 상태
   - 향후 이메일 재개할 경우, 웹매거진 콘텐츠에서 이메일용 HTML 생성하는 방식 설계 필요

---

## 의사결정 로그

| 결정 | 선택 | 이유 |
|---|---|---|
| 유실 콘텐츠 복구 방법 | trycloudflare URL에서 HTML 다운로드 | URL이 살아있을 때 긴급 백업 필요. 소스는 watcha.io에 있어서 접근 불가. |
| 배포 위치 | 기존 kletter.vercel.app 덮어쓰기 | 어차피 뉴스레터 → 웹매거진 피벗. 도메인 유지가 자연스럽고, 구버전은 git 히스토리에 남음. |
| URL 구조 | 폴더 + index.html (`weekly/2026-W17/index.html`) | Vercel 기본 동작으로 `/weekly/2026-W17` 확장자 없이 서빙 가능. vercel.json 설정 불필요. |
| 스케줄 정지 방식 | `enabled: false` (삭제 아님) | 재사용 가능성 남김. 삭제는 API로 불가해서 웹 UI에서만 가능. |
| 스케줄 재활성화 시점 | 웹매거진 프로세스 설계 후 | 현재 프롬프트는 구버전 구조 가정. 그대로 돌리면 덮어쓰기 사고 가능. |

---

## 알려진 이슈 & 기술 부채

### 해결됨
1. ~~**웹매거진 콘텐츠 유실 위험**~~ → ✅ HTML 백업 + Vercel 배포 완료
2. ~~**스케줄 에이전트 덮어쓰기 위험**~~ → ✅ 정지 완료

### 진행 중
3. **원본 소스 코드 없음** — watcha.io 접속 전까지 미해결. 지금 Vercel에 올린 건 "렌더링된 결과물"일 뿐 진짜 소스(템플릿/데이터)가 아님.
4. **스케줄 에이전트 미완성** — 정지만 해둔 상태. 웹매거진용 새 프롬프트 설계 필요.

### 기술 부채
5. **루트의 구버전 파일** — `newsletter_2026_W16.*` 파일들 정리 안 됨
6. **subscribe 페이지 CTA** — 뉴스레터 포지셔닝이었던 문구가 웹매거진과 어색할 수 있음 (미확인)
7. **이메일 발송 프로세스 부재** — 피벗 후 이메일 재개 여부 미정

---

## 커밋 히스토리 (이번 세션)

```
a86fa30 웹매거진 피벗 병합 (merge commit)
5e0250f 웹매거진 피벗 — 뉴스레터 단일 HTML → 다중 페이지 구조 전환
```

---

## 파일 참조

- **백업 원본**: `~/kletter-shortform-backup/` (trycloudflare URL에서 다운로드)
- **배포 중인 파일**: `~/kletter-newsletter/deploy/` (Vercel root)
- **이전 워크로그**: `docs/worklog/2026-04-16.md`
- **리서치**: `docs/research/newsletter_platform_comparison.md`

---

## 현재 구독자 목록 (변동 없음, 6명)

| 이메일 | 비고 |
|---|---|
| kay.kim@watcha.com | 프로젝트 오너 |
| izzie@watcha.com | 사내 |
| eleven@watcha.com | 사내 |
| selina@watcha.com | 사내 |
| tpark@watcha.com | 사내 |
| eden@watcha.com | 사내 |
